SELECT 
' ' AS medrecno ,
MM.FIRST_NAME AS fname,
' ' AS mname,LEFT(MM.LAST_NAME,100) AS lname,
' 'AS suffix,' ' AS pref_name,TO_CHAR(MM.DOB,'MM/DD/YYYY') AS dob,
MM.SSN AS ssn ,
 LEFT(MM.CURRENT_MEDICAID,25) AS cin,
' ' AS din, 
' ' AS nysid,
MM.GENDER AS birth_sex,
' ' AS gender,
' ' AS race_cd,
' ' AS ethniccd,
IFF(MM.ADDRESS_LINE2 IS NULL,MM.ADDRESS_LINE1, CONCAT(MM.ADDRESS_LINE1 ,' ,',MM.ADDRESS_LINE2)) AS address,
MM.CITY AS city ,
MM.STATE AS state ,
LEFT(MM.ZIP_CD,5) AS zipcode 
FROM  DLAKE.CHOICE.FACT_MEMBER_MONTH AS MM  
WHERE 
MM.ORIG_ENROLLMENT_START_DT ='2024-02-01'
AND MM.REPORTING_MONTH_DATE =MM.ENROLLMENT_START_DT 
AND MM.PROGRAM ='SH'
ORDER BY MM.SUBSCRIBER_ID 
